# Example .env file
# For production, replace with a Postgres connection string and update db.js accordingly.
# Example for Postgres: DATABASE_URL=postgresql://user:pass@host:5432/dbname

# For local testing (SQLite is used by default)
DATABASE_URL=
BASE_URL=http://localhost:3000
PORT=3000
